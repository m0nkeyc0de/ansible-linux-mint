---
- name: Take care of Linux Mint 22 Workstation
  gather_facts: true
  connection: local
  hosts: all

  tasks:
  - name: User tweaks
    ansible.builtin.include_role:
      name: tweaks_user
    tags: always

